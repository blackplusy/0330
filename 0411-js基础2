js的数据类型
	字符类型
	数值类型
	布尔类型
	复合数据类型
		常用内置对象
		日期对象
		全局对象
1.复合数据类型
	a.数学对象
	封装了和数学相关的特性，包含一些常数和数学函数
	调用Math对象的方法
	math.[{属性(方法名)}]
		PI    返回圆周率
		abs   返回数字的绝对值
		sin   返回数的正弦
		min   返回给定数字中最小值
		max   返回给定数字中最大值
	b.字符串对象
	分装了和字符串相关的特性，用来处理字符串，可以对字符串进行剪切、合并等等操作
	调用string对象方法或者属性
		string([val])
			val 是可选项
	c.数组对象
	js中基本数据类型，封装和数组相关的方法和属性
	房间1    房间2   房间3   房间4
	范冰冰   刘亦菲  林志玲   乔丹
	创建数组的方式
		1.通过new运算符调用Array对象构造函数
			var a=new Array()
		2.通过构造函数传递元素为参数(创建带指定元素的数组)
			var a=new Array(10,20,'simida','haleshao')
		3.直接将元素放入[]中，元素通过逗号分隔开
			var a=[1,2,3,4]
		4.创建指定数组(元素个数)
			var a=new Array(3)
2.其他数据类型
	a.函数
	函数的角色有两种，一种是数据类型，一种是程序设计
	定义函数的方式
		function 函数名(参数)
		{
			this.参数=参数;
		}
	b.空值
	之前所说的数据类型都有自己的内容，编程中还有一种：什么都没有(null)
	一个=代表赋值，两个==代表比较，三个===代表对象
	c.不确定数据类型
	undefined 表示一个变量什么都没有得到，连空都没有
	应用里面null和undefined实际意义相同
3.数据类型的转换
	a.隐式数据类型转换
	程序在运行时候根据上下文需要自动将数据转换为另外一种数据类型过程
	b.显式数据类型转换
	通常用parseInt、parseFloat
4.常量和变量
	a.常量
		从始至终不能被改变的数据
		常量的种类
		包括字符类型、布尔类型，数值类型，null等等
		"天儿不错"  字符常量
		077        数值常量
		true       布尔常量
	b.变量
		b1.什么是变量
			变量是程序中可以发生改变的量，专业说法是可读写的内存单元
			可以理解为装东西的容器，变量名代表分配给他们的内存单元
		b2.变量的定义方式
			var 变量名=值   定义变量
			变量名=值       赋值
			var 变量名      定义变量
			字符类型变量
				var str="hello"
			数值类型变量
				var b=10
			布尔类型变量
				var t=true
			复合类型变量
				var obj=new object()
		b3.变量的命名规则
			变量名必须以字母或者_或者$开头，后面可以跟数字或者字符，不能使用特殊符号
			js对标识符大小写敏感
		b4.变量的作用域
			作用域，有效范围js变量作用域有全局和局部之分，变量名同名后局部作用于优先于全局
5.运算符和表达式
	a.表达式
		产生结果的式子，表达式可以作为参数传递给函数或者将表达式赋值给变量
	b.操作数
		表达式中的常量或者变量
		(a+b)/c+d
	c.运算符
		表达式中起计算作用，如果在数学中就是加减乘除
		c1.算数运算符
			+
			-
			*
			/
			%
			加法操作符使用数字符号返回两个操作数的和，操作数据类型要求整型，如果是字符串另外意思
		c2.关系运算符
			比较两个操作数大于小于等于的运算符，返回的值是布尔类型
			1>相等运算符,判断两个操作数是否相等
				如果相等返回真，如果不等返回假
			2>等同运算符
				==并非严格意义上的相等性判断，通过数据类型转换也可以返回真
				===严格意义上相等，他们的值和数据类型完全一致才能返回真
			3>不等运算符
				使用！=返回一个布尔值判断两个操作数是否不相等，如果不相等返回真
			4>不等同运算符
				!==效果和等同运算符刚好相反，严格不相等返回真
			5>比较运算符
				两个操作数的比较，返回值只能是真或者假
				>
				<
				==
				>=
				<=
				!=
			6.in运算符
				in运算符检查对象是否有指定属性，通常数组中存在元素索引集合，每个元素是非负整数
			7.instanceof运算符
				返回一个布尔值，表明对象是否是某个类的实例
		c3.逻辑运算符
			c31>逻辑与
				操作符&&,属于双目运算符，操作数被当成布尔类型名，可以是变量也可以是常量
			c32>逻辑或
				操作符||,属于双目运算符.对两个操作数进行或运算
			c33>逻辑非
				操作符!属于单目运算符.对操作数逻辑值取反，可以变量也可以是常量
		c4.其他运算符
			c41>类型检测运算符
				js中的数据都有数据类型通过typeof获取到数据类型的名字
				typeof的返回值6种可能
					Number
					String
					Boolean
					Object
					Function
					null
			c42>条件运算符
				可以根据条件在两个语句中选择一个来执行，三目运算符
				条件表达式？语句1:语句2
				满足条件执行语句1，不满足执行语句2
			c43>数组存取运算符
			c44>void运算符
				有些地方不需要返回值，比如js代码写入浏览器执行
			c45>delete运算符
				delete运算符可以删除对象的一个属性或者数组的一个元素，js对象属性可以动态添加和删除
			c46>this运算符
				准确说是一个关键字，可以理解为运算符，面向对象中可以返回当前对象的引用


