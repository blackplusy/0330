js的数据类型
	字符类型
	数值类型
	布尔类型
	复合数据类型
		常用内置对象
		日期对象
		全局对象
1.复合数据类型
	a.数学对象
	封装了和数学相关的特性，包含一些常数和数学函数
	调用Math对象的方法
	math.[{属性(方法名)}]
		PI    返回圆周率
		abs   返回数字的绝对值
		sin   返回数的正弦
		min   返回给定数字中最小值
		max   返回给定数字中最大值
	b.字符串对象
	分装了和字符串相关的特性，用来处理字符串，可以对字符串进行剪切、合并等等操作
	调用string对象方法或者属性
		string([val])
			val 是可选项
	c.数组对象
	js中基本数据类型，封装和数组相关的方法和属性
	房间1    房间2   房间3   房间4
	范冰冰   刘亦菲  林志玲   乔丹
	创建数组的方式
		1.通过new运算符调用Array对象构造函数
			var a=new Array()
		2.通过构造函数传递元素为参数(创建带指定元素的数组)
			var a=new Array(10,20,'simida','haleshao')
		3.直接将元素放入[]中，元素通过逗号分隔开
			var a=[1,2,3,4]
		4.创建指定数组(元素个数)
			var a=new Array(3)
2.其他数据类型
	a.函数
	函数的角色有两种，一种是数据类型，一种是程序设计
	定义函数的方式
		function 函数名(参数)
		{
			this.参数=参数;
		}
	b.空值
	之前所说的数据类型都有自己的内容，编程中还有一种：什么都没有(null)
	一个=代表赋值，两个==代表比较，三个===代表对象
	c.不确定数据类型
	undefined 表示一个变量什么都没有得到，连空都没有
	应用里面null和undefined实际意义相同
